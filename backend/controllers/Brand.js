const tradeBrands = require("../models/Brands")
const User = require("../models/user")

const mongoose = require("mongoose")

// I need to add picture about my brand product
const PhotoBrand = async(req, res) => {

}



module.exports ={
    PhotoBrand
}